<div class="table-@yield('type', 'light')">
	<table 
		id="@yield('id', 'datatable')" 
		class="table table-striped table-bordered dataTable"
		data-caption="@yield('title', _('Tableau'))"
	></table>
	<div class="table-bottom"></div>
</div>
